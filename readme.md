# Installation

`yarn install`

or

`npm install`

# Configuration

Copy `env.sample` to `.env` and update the PRIVATE_KEY variable

# To run the auto compounder

`yarn run start`

or

`npm run start`
